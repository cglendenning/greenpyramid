# Green Pyramid project rules

Follow [AGENTS.md](AGENTS.md) for the private specification, focused implementation briefs, contract validation and acceptance evidence workflow.

Read the specification through its index and focused briefs; do not load it into context in full. The existing `.claude/hooks/guard-spec-read.sh` enforces scoped reads.

Never commit secrets or print `lib/services/secrets.dart`.
